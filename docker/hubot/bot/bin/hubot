#!/bin/sh
set -e

npm install
export PATH="node_modules/.bin:node_modules/hubot/node_modules/.bin:$PATH"

main() {
  local script_dir=$(cd $(dirname $0); pwd)
  ${script_dir}/coffee node_modules/.bin/hubot "$@"
}
main

